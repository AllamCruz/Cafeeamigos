import React from 'react';
import MenuDisplay from '../components/Menu/MenuDisplay';

const Home: React.FC = () => {
  return (
    <div>
      <MenuDisplay />
    </div>
  );
};

export default Home;